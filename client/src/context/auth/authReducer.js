import {
  AUTH_ERROR,
  LOGIN_SUCCESS,
  REGISTER_FAILED,
  REGISTER_SUCCESS,
  LOGIN_FAIL,
  LOGOUT,
  USER_LOADED
} from "../types";
const AuthReducer = (state, action) => {};

export default AuthReducer;
